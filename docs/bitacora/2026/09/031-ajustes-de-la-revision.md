# 031 · Ajustes de la revisión, uno por uno

**Fecha:** 2026-09-14 · **Rama:** trabajo sin commit sobre `main` (el founder decide el PR) · **Pieza:** OL-014

El founder pidió aplicar los ajustes del informe ([REVISION_2026-09-14.md](../../ops/REVISION_2026-09-14.md)) uno por uno; lo del inicio vacío queda fuera porque él está poblando la agenda en otro chat. Cada paso se verificó en pantalla (390×844 y 1280×800) antes del siguiente.

## Hecho (en orden)
1. **S1 · Cualquier cuenta podía hacerse admin.** Migración `20260914080000_rol_solo_admin.sql` **aplicada a producción**: `revoke update (rol)` a anon y authenticated, y trigger `proteger_rol` que exige `es_admin()` para cambiar el rol. El panel no cambia roles (se ponen a mano), así que nada se rompe. `avisos_correo_motivo` no se restringe: la propia persona lo pone a null al reactivar sus avisos.
2. **A1 · Conteo de "voy" en la base.** RPC `van_por_evento(ids uuid[])` (migración `20260914090000`, aplicada); la agenda ya no trae todas las asistencias de la historia. La consulta de eventos filtra por ciudad (`.eq("ciudad")`), y el chip de ciudad, con una sola ciudad, es texto sin caret ni hoja.
3. **M3 · Seguidores con `count`** y "¿lo sigo?" con `maybeSingle` en las fichas de lugar y artista; las asistencias de Mi perfil y de la persona se filtran en la consulta (`eventos!inner` + `gte`), no en JS.
4. **M1 · Cabeceras de seguridad** en `next.config.ts`: `Content-Security-Policy: frame-ancestors 'none'`, `X-Frame-Options: DENY`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`. Sin CSP completa (Next y Mapbox exigen inline/eval/blob y una CSP mal afinada rompe el mapa sin avisar). Verificadas con `next build` + `next start` en el puerto 3100.
5. **UX 2 · Renglones sin negaciones repetidas.** La línea de calendario solo aparece cuando hay fecha; el hueco de la foto lleva el icono de lo que es (disciplina, pin del lugar, calendario del evento) sobre `--fondo-miniatura`, en vez de un cuadro punteado que promete una foto.
6. **UX 3 · Orden alfabético real.** `compararNombres` (sin acentos, mayúsculas ni signos) en artistas y lugares: "¡Caracoles!" va en la C. Migración `20260914100000_nombres_sin_comillas.sql` (aplicada): dos nombres del CAPO envueltos en comillas quedan limpios; el importador ya las quita al leer.
7. **UX 6 · Esqueleto de las pantallas raíz** (`ui/CargandoRaiz`): barra, campo y tres renglones con la forma de la lista, y la nav inferior en su sitio; `loading.tsx` propio en Artistas (antes usaba el de la raíz, "Cargando la agenda…").
8. **Founder 4 · Avatar redondo en la ficha de artista.** `Cartel` con `forma="avatar"` (112 px, redondo, sin lupa; el toque sigue abriendo la foto entera). Pendiente de verse con una foto real: ningún artista del CAPO la tiene.
9. **Founder 2 · Las fichas respetan el shell.** `Ficha .pagina` en `--fondo-contenido`; la cabecera interior (`ui/Barra .interior`) es una franja blanca de borde a borde, pegajosa arriba, con borde; la barra de acciones sigue blanca abajo. Para que la franja llegue al borde desde dentro del gutter, `--al-centro` pasa a calcularse sobre `100vw` (vale lo mismo en cualquier caja). Los títulos pegajosos de las fichas quedan bajo la cabecera y en el tono del contenido.
10. **Founder 1 · Estados del renglón.** Hover (con ratón): fondo blanco, marca de 3 px del color de acción a la izquierda y título en ese color, con transición corta; pulsado igual sin marca; elegido (`aria-current="true"`) en `--primario-suave`; foco de teclado con aro interior.
11. **UX 5a · Reclamar la ficha en un solo sitio.** En las fichas del CAPO sin dueño, un bloque bajo la etiqueta: "Ficha por confirmar, tomada del Catálogo de Artistas Potosinos. ¿Eres tú? Reclámala: la editas, le pones foto y publicas tus fechas" con el botón secundario "Reclamar esta ficha" (`EsMiNombre comoBoton`); desaparece del menú ··· y del pie en esas fichas. Las fichas registradas por otra persona (sin origen) conservan "Soy yo / es mi grupo" en el menú.
12. **UX 7 · Sin dos negaciones.** Con cero seguidores y cero fechas, las fichas de artista y lugar muestran una sola línea en gris ("Sin fechas próximas · Nadie lo sigue todavía"; `Ficha .dato .suave`). Y "Publicar una fecha de <nombre>" pasa a "Publicar una fecha".
13. **Founder 3 · Aviso de privacidad y reglas de uso.** Páginas `/privacidad` (LFPDPPP: responsable, datos, fines, catálogos públicos, con quién se comparten, derechos ARCO, cookies) y `/reglas` (qué se publica, qué no, qué hace el administrador, tu cuenta), enlazadas desde Entrar ("Al entrar aceptas…"), el pie de Mi perfil y el pie de los correos. **Para el founder:** el contacto es oscar@agenciaparadigma.com (decidido el 2026-09-14: el dominio no recibe correo, sin MX; si algún día se quiere hola@somosnosotros.org, reenvío con ImprovMX: dos MX y `include:spf.improvmx.com`); si puede, una lectura legal.
14. **UX 4 · Borrador en el alta de evento** (`somosnosotros:borrador-evento`): título, cuándo, dónde, quién, cuánto y más detalles se guardan en el teléfono con cada cambio y se restauran al volver; se sueltan al publicar. "¿No está en la lista? Regístralo y vuelves aquí con él elegido": el alta de lugar recibe `?siguiente=/eventos/nuevo`, el regreso dice "Volver" y al publicar el lugar `crearLugar` vuelve al alta con `?lugar=<id>` ya elegido (la URL manda sobre el borrador). Probado con un usuario desechable: el título sobrevive al ir y volver.
15. **M2 · Tope de avisos por autor.** `avisarNuevoEvento` cuenta los eventos del autor en 24 h; a partir del cuarto, el evento se publica pero no avisa a los seguidores (`TOPE_AVISOS_POR_AUTOR_DIA = 3`, con una línea en el log).
16. **A2 · Filtros en la URL, filtro y paginación en el servidor (cierra OL-013).** `/artistas?hace=musica&que=jazz%2C+blues+y+soul&q=beto&n=200`: disciplina, detalle, lo escrito y cuántos se ven viven en la URL (se comparte, sobrevive al volver atrás). Migración `20260914110000_orden_y_filtros.sql` (aplicada): columna `nombre_orden` en artistas y lugares, llenada por trigger con `normalizar_nombre` (que es `stable`, no sirve para columna generada), índices `(ciudad, nombre_orden) where visible`, y RPC `disciplinas_con_artistas` y `detalles_de_disciplina` para los chips. La página trae los artistas con fecha próxima que cumplen el filtro (todos) y luego 100 alfabéticos (`range`), con "Ver más (421 más)" que pide otros 100; el conteo viene de `count: "exact"`. La búsqueda (`ui/Buscador`, cliente) escribe `?q=` 300 ms después de dejar de teclear sin perder el foco; los chips son enlaces (`ChipEnlace`). `ListaArtistas` deja de ser cliente. En Lugares el tipo va en la URL (`?vista=lista&tipo=foro`) y sigue filtrándose en el navegador (el mapa necesita todos los puntos; 500 lugares caben). Probado: 521 → "Música · Jazz" 23 → "beto" 2; "Ver más" → `n=200`; tipo=foro → 4 lugares en mapa y lista.17. **A3 · La misma lógica, una sola vez.** `lib/supabase/sesion.ts` (`sesionOEntrar`, antes copiada en eventos, lugares y artistas), `lib/formulario.ts` (`limpiar`, antes en cuatro libs; `esUuid`, antes una regex en trece archivos), `lib/geo.ts` (`distanciaKm`, antes también `kmEntre` en lugares) y `lib/subirFoto.ts` (validar 5 MB → reducir → subir → URL pública, antes copiado en los formularios de perfil, lugar, artista y evento). Sin unificar fichas ni formularios completos: son distintos de verdad.
18. **Mejoras rápidas de UX y `global-error`.** Hojas inferiores (`ui/Hoja`, instalar, ciudad) del ancho de la columna y centradas en escritorio; objetivos de 44 px en "ver" de las fichas, "más" del desplegable y el avatar de la barra; la nota "el iPhone lo ofrece encima del teclado" solo en iOS (`@supports (-webkit-touch-callout: none)`), "Tecléalo o pégalo" en el resto; "Cómo llegar" deshabilitado dice "sin dirección"; en el alta de artista, "Música · cámbialo si no es" hasta que la persona elija; `src/app/global-error.tsx` en español (antes, la página inglesa de Next si fallaba el layout raíz). No mirados en pantalla (exigen sesión o un evento sin dirección): tipos y build en verde.
19. **Reclamar la ficha, discreto y al final (corrección del founder).** El bloque de arriba sale. Al final de la ficha del CAPO, **solo con sesión** (sin sesión no se ofrece nada, para no invitar a reclamos ajenos), un letrero en gris subrayado "¿Eres tú o tu banda?" que abre la hoja; la hoja dice el origen ("Esta ficha se tomó del Catálogo… y está por confirmar"), qué se gana y las dos salidas. El pie "Ficha tomada del Catálogo…" desaparece de la ficha de artista (queda en la de lugar).
20. **Cuándo como en el calendario del teléfono (referencia iOS del founder).** `SelectorCuando` pasa a dos renglones: Empieza · [fecha] [hora] y Termina · [fecha] [hora] · ✕, con píldoras que abren el selector nativo (`ChipNativo`), y la frase de confirmación debajo ("martes 15 de septiembre · 19:00 a 21:00"). Fuera los chips Hoy/Mañana, 18/19/20 y Sin fin/2 h. Al mover el inicio, el fin se mueve con la misma duración; "Sin hora de fin" abre la hora. Probado con sesión: poner 21:00 deja "Termina 15 sep 2026 · 9:00 p.m.".
21. **Cola:** favicon e icono de la app para "Añadir a inicio" (hoy `icono-192.png` y `apple-touch-icon.png` son los de la Fase 0; el founder pide diseñarlos con SMSNSTRS).
22. **El menú ··· quedaba detrás de la barra de acciones (aviso del founder).** Causa: la cabecera interior pegajosa con `z-index: 10` crea un contexto de apilamiento y la hoja del menú vivía dentro, así que nunca superaba ese 10 y la barra fija (20) la tapaba. Arreglo de raíz: `ui/Hoja` se pinta al final del `body` con un portal (`createPortal`, montada con `useSyncExternalStore` para no chocar con la hidratación); ninguna cabecera ni barra puede taparla, abra desde donde abra. Comprobado en la ficha de Vértika.
23. **Sin la frase repetida en Cuándo** ("martes 15 de septiembre · 19:00 a 21:00"): las píldoras ya lo dicen; queda solo "Esa hora ya pasó" cuando aplica.
24. **Sin pie de origen tampoco en la ficha de lugar** (founder). `PieOrigen` desaparece del código; el origen del catálogo se menciona solo en la hoja de reclamar de los artistas y en el aviso de privacidad.

## Verificación
Lint, typecheck, 102 pruebas y build en verde tras cada paso. Un usuario desechable (creado con la llave de servicio y borrado al final) sirvió para probar con sesión el alta de evento, el regreso desde registrar lugar, Mi perfil y el alta de artista. Mirado: agenda (chip de ciudad como texto), lista de artistas y de lugares (orden, iconos, sin "Sin fechas"), fichas de lugar y artista (tres franjas, a 390 y 1280), Entrar (cabecera), hover en escritorio. Cabeceras HTTP comprobadas con `curl` sobre la build.

## Pendiente del informe (siguientes pasos)
Ligar la ficha sola cuando la persona entra con el correo del CAPO (UX 5b); tipos generados de la base (M1); escrituras fuera del render al volver de Entrar (M2 arquitectura); `Boton` con `href`, un solo chip y un solo buscador en Agenda (M5); pruebas de `avisos.ts` (M6); OPEN_LOOPS a tres renglones por OL y poda de `docs/heredado` (M7/B8); las mejoras rápidas de UX que quedan (hojas de 1280 px, objetivos de 44 px, nota de iPhone, "Qué hace" resuelto en silencio, "Cómo llegar" sin explicación).
