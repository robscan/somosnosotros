# Asignaciones del gestor

Registro central de reservas, actualizado el 2026-09-18. Solo el gestor asigna
o cambia rama, base, OL, bitacora y alcance. El operador consulta este registro
ademas de ejecutar `scripts/ops/siguiente-bitacora.sh`: el script comprueba
archivos existentes, pero no sustituye reservas aun sin archivo.

| Pieza / tarea | Rama y base | OL / bitacora | Archivos o recursos asignados | Limite |
| --- | --- | --- | --- | --- |
| Pincel / Analiza activacion de lienzo colabor (`01a0b601-b973-7983-95eb-fe66f43e1988`) | `codex/pincel-prototipo`, base `d1b5bdc` | OL-084 / 118 | `experiments/pincel-prototipo/**`, bit118, entrada propia OL-084 | Prototipo local. Presentacion y VoBo del founder; sin app, backend, push ni produccion. |
| Directorios / Anadir slider de eventos proximos (`01a0b61e-abcd-7483-a141-116715ad40d4`) | `codex/directorios-alfabeticos`, base `eb2f80e` | OL-085 / 119 | Directorios Artistas/Lugares, indice y carriles, helpers/tests afectados, bit119, entrada propia OL-085 | PR105 draft. Propuesta vertical y carriles segun ultima decision, primero VoBo; no publicar propuesta anterior. |
| Activacion de avisos / Operador: avisos y anuncios fiables (`01a0b694-ed57-7700-b717-a4e56dbd1c31`) | `codex/avisos-operacion`, base `eb2f80e` | OL-086 / 120, reservados por gestor | Bit120 y entrada propia OL-086; operacion autorizada de avisos_config, cron/Vault y transicion conforme bit113 | Sin codigo nuevo, nuevas migraciones, Auth, Storage, CAPO ni planes. Propiedad exclusiva de esa ventana remota hasta informe de liberacion. |
| Reactivar avisos / operador Claude nuevo (Sonnet 5, esfuerzo medio) | `codex/avisos-operacion` en `/Users/apple-1/somosnosotros-avisos-operacion`, sobre `eb2f80e` | OL-086 / 120 (continúa) | Bit120 y entrada OL-086; lectura de producción; Vercel solo si el founder da acceso | Reunir la prueba de que los despliegues antiguos no emiten y preparar las dos rondas diarias. No reactivar `entregar` ni el cron sin las horas del founder y la revisión del gestor. |
| Security Advisor, lo que queda / chat «SEO, tags y Google Analytics» (Sonnet 5, esfuerzo medio) | `advisor-seguimiento`, base main del día | OL-077 / 121 | Bit121, entrada OL-077; migración nueva que solo añade o endurece, con nombre dado por el gestor | Leer el Advisor en producción solo lectura, arreglar lo que quede en una migración. No aplicarla: la aplica el gestor. |
| Topes de campos / chat «Cámara y fecha en evento» (Haiku 4.5) | `topes-de-campos`, base main del día | OL-065 / 096 | Los tres formularios del canon y su validación; bit096 y entrada OL-065 | Que la pantalla acote lo mismo que el servidor, sin quitar texto ya escrito. Sin migración. |
| Cabeceras consistentes / chat «Inconsistencia en búsqueda y filtros» | `cabeceras-consistentes`, base main tras unir `codex/directorios-letra-filtro` | OL-087 / 122, reservados por gestor | Cabecera única en Agenda, Lugares y Artistas (prototipo firmado por el founder 2026-09-18), botón ↑, renglón 1 que se esconde al bajar; prototipo en `docs/rediseno/prototipos/cabeceras.html`; bit122 y entrada OL-087. Sin migración |
| Pincel en la app / chat «Ajustar el controlador de Pincel (OL-084)» (Sonnet 5) | `pincel-app`, base main del día | OL-088 / 123, reservados por gestor | Llevar a la app el prototipo firmado de OL-084 (codex/pincel-prototipo 6063e16): primero un plan con fases y migraciones para firmar; después el código. Autorizado por el founder 2026-09-19 («llevalas a prod», «que comience producción») |
| Mapa en fichas / chat «Terminar los directorios alfabéticos (PR #105)» (Sonnet 5) | `mapa-en-fichas`, base main del día | OL-089 / 124, reservados por gestor | Mapa de referencia en la ficha de cada lugar y evento (pedido del founder 2026-09-19), respetando direcciones reservadas; bit124 y entrada OL-089. Sin migración |
| Barra inferior despegada / chat nuevo «Barra inferior despegada (OL-090)» (Sonnet 5, esfuerzo medio) | `nav-inferior-pegada`, base main del día (`a29e887` o posterior) | OL-090 / 125, reservados por gestor 2026-09-21 | Bug del founder (2026-09-21): en la app instalada en el iPhone la barra de navegación inferior se despega del borde de abajo, derivado de los últimos ajustes (cabecera única, tira de letras, mapa en fichas). `NavInferior` y su CSS, `ui/Cabecera`, `TiraLetras`, `globals.css` solo si la causa está ahí; bit125 y entrada OL-090. Sin migración | Corrección, no rediseño: reproducir en el simulador de iPhone (Safari y app instalada), causa medida antes de tocar, arreglo mínimo. Entrega local al gestor; publica el gestor. |
| Grafo cultural y cola de piezas / el propio gestor («Gestor de cambios II») | `main`, solo documentos | OL-091 / 126, reservados por gestor 2026-09-21 | `docs/rediseno/24-grafo-cultural.md`, `docs/ops/COLA_DE_PIEZAS.md`, bit126 y entrada OL-091 | Propuesta para firma del founder. `DEFINICION.md` no se toca hasta su firma. Las piezas de la cola toman su OL y bitácora al encargarse (tras la primera tanda, el siguiente libre es OL-116 / 151). |
| A1 · Lectura del cartel / chat nuevo «Lectura del cartel (OL-092)» (Sonnet 5, esfuerzo medio) | `cartel-lectura`, base main del día | OL-092 / 127, reservados por gestor 2026-09-21 | L16, L34 y primera mitad de L37 de `COLA_DE_PIEZAS.md`: `src/lib/cartel.ts`, la acción que lo llama y cómo el alta de evento recibe artistas y autor; medir qué modelo de IA basta (hoy `claude-opus-5`). No toca la maquetación del alta (eso es A7). Sin migración salvo que la causa lo exija | Arreglo mínimo con causa medida antes de tocar; sin rediseño. Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. El cambio de modelo se propone con medición; lo decide el founder. |
| A2 · Números de la agenda / chat nuevo «Números de la agenda (OL-093)» (Sonnet 5, esfuerzo medio) | `agenda-numeros`, base main del día | OL-093 / 128, reservados por gestor 2026-09-21 | L1 y L27: contadores de «Coincidencias» y «Publica la comunidad» frente a sus listas, y el carril de destacados que omitió un evento de hoy; consultas de la agenda y sus pruebas. Producción solo lectura | Arreglo mínimo con causa medida antes de tocar; sin rediseño. Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. |
| A3 · Gestos que se disparan solos / chat nuevo «Gestos al deslizar (OL-094)» (Sonnet 5, esfuerzo medio) | `gestos-deslizar`, base main del día | OL-094 / 129, reservados por gestor 2026-09-21 | L10 y L45: `ui/Deslizable` y los renglones que lo usan; carril de `Destacados`. Redefine el canon del gesto y lo deja escrito en su bitácora. Sin migración | Arreglo mínimo con causa medida antes de tocar; sin rediseño. Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. Prueba en el simulador de iPhone, con el dedo, no solo con ratón. |
| A4 · Cercanos y ubicación / chat nuevo «Cercanos y ubicación (OL-095)» (Sonnet 5, esfuerzo medio) | `cercanos-ubicacion`, base main del día | OL-095 / 130, reservados por gestor 2026-09-21 | L25, L36 y L50: cuándo y cuántas veces se pide la ubicación, por qué se vuelve a pedir, y que Cercanos ordene por la ubicación de la persona aunque el chip diga otra ciudad (regla «el contexto ordena, no limita»). No toca `ui/Cabecera` salvo lo imprescindible (OL-090 trabaja ahí). Sin migración | Arreglo mínimo con causa medida antes de tocar; sin rediseño. Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. L50 se entrega como medición. |
| A5 · Arreglos chicos de artistas / chat nuevo «Arreglos chicos de artistas (OL-096)» (Haiku 4.5, esfuerzo bajo) | `artistas-arreglos-chicos`, base main del día | OL-096 / 131, reservados por gestor 2026-09-21 | L17, L24, L30 y L35: enlace de Instagram desde @usuario, carril de enlaces de la ficha, quitar la foto de un artista (vuelve el símbolo SN) y reclamo que no se duplica. L47 NO va aquí (pieza A8). Sin migración salvo que L35 pida una restricción que solo añade | Arreglo mínimo con causa medida antes de tocar; sin rediseño. Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. Corregir el dato de la foto reportada es de producción: lo hace el gestor con permiso del founder. |
| Criterio de obras colectivas / chat del founder «Motor de experiencias interactivas» (`local_f5a28e38`, Fable 5.1: lo abrió el founder) | `obras-colectivas-criterio`, base main del día, en worktree propio (no en la carpeta principal) | OL-097 / 132, reservados por gestor 2026-09-21 | Solo documentos: `docs/rediseno/25-obras-colectivas-criterio.md`, bit132, entrada OL-097 y su línea en «Decidido» con las palabras del founder («Firmo, adelante»): Pincel es la primera obra colectiva; el motor no se construye hasta la segunda obra; cuatro ajustes al plan de OL-088 | No toca la rama `pincel-app` ni código. Pincel sigue en pausa hasta que el founder lo pida al gestor. Nota: la migración `20260919030000_obras_colectivas.sql` habrá que renombrarla con fecha posterior a `20260921100000` (reservada para OL-093) antes de aplicarla. |
| Texto al compartir sin ciudad / chat nuevo «Texto al compartir sin ciudad (OL-098)» (Haiku 4.5, esfuerzo bajo) | `compartir-sin-ciudad`, base main del día | OL-098 / 133, reservados por gestor 2026-09-21 | Pedido del founder (2026-09-21): quitar «San Luis Potosí» del texto con que se comparte el sitio, para acompañar el crecimiento a otras ciudades. `TEXTO_INVITAR` en `src/lib/perfil.ts`, las descripciones de `src/app/layout.tsx` (Open Graph, Twitter, descripción general) y el manifest si la nombra; sus pruebas. Sin migración | Solo textos; no toca fichas, SEO por ciudad ni DEFINICION. Entrega local al gestor; publica el gestor. |
| Pincel en la app, reanudación / chat nuevo «Pincel en la app, reanudación (OL-088)» (Sonnet 5, esfuerzo medio) | `pincel-app` en `/Users/apple-1/somosnosotros-pincel-app` (hereda 5c42f64; trae main primero) | OL-088 / 123 (continúa) | Reanudado por el founder el 2026-09-21. Primero los cinco ajustes del doc 25; migración renombrada a `20260922090000_obras_colectivas.sql`; luego el plan por fases | Una fase a la vez, entrega y revisión entre fases. Migraciones las aplica el gestor con permiso del founder. El operador anterior (`local_8b73c722`) ya no sigue. Fase 1 aprobada por el founder el 2026-09-21; Fase 2 autorizada tras la corrección de maquetación (rama `pincel-crear-obra-maquetacion`). Migración reservada para el borrado de obras cerradas: `20260922120000_obras_colectivas_borrado.sql`. |
| A6 · Arreglos chicos de agenda y evento / chat nuevo «Chip de fecha y precio numérico (OL-099)» (Haiku 4.5, esfuerzo bajo) | `agenda-evento-arreglos-chicos`, base main del día | OL-099 / 134, reservados por gestor 2026-09-21 | L6 (chip de fecha que dice «Hoy» y muestra todo: estado vacío) y L33 (precio solo numérico, en pantalla y en servidor). Sin migración | Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. El `.env` real nunca entra a la carpeta del operador. |
| A7 · Lugar y dirección en el alta de evento / chat nuevo «Lugar y dirección en el alta de evento (OL-100)» (Sonnet 5, esfuerzo medio) | `alta-evento-lugar`, base main del día | OL-100 / 135, reservados por gestor 2026-09-21 | L2, L3 y segunda mitad de L37: teclado que tapa el botón del cartel, «Confirmar» en vez de «Falta», ayuda bajo el campo y no dentro del botón, sugerencias de dirección según dónde está la persona. **Prototipo firmado antes que código**; el código empieza cuando A6 esté en main (comparten `FormularioEvento`) | Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. El `.env` real nunca entra a la carpeta del operador. |
| A8 · Subcategorías de disciplina / chat nuevo «Subcategorías de disciplina (OL-101)» (Sonnet 5, esfuerzo medio) | `subcategorias-disciplina`, base main del día | OL-101 / 136, reservados por gestor 2026-09-21 | L47 con la anotación del founder: elegir cualquier subcategoría que ya existe y proponer una nueva cuando no existe, en alta y edición de artista. **Prototipo firmado antes que código.** Prototipo firmado por el founder el 2026-09-21 («apruebo», c32af33). Migración reservada: `20260922110000_subcategorias_por_disciplina.sql` (una función de solo lectura) | Entrega local al gestor; publica el gestor. Sin council, workflows ni subagentes. El `.env` real nunca entra a la carpeta del operador. |
| Coincidencias abre sus eventos / chat nuevo «Coincidencias abre sus eventos (OL-102)» (Sonnet 5, esfuerzo medio) | `coincidencias-lista`, base main del día | OL-102 / 137, reservados por gestor 2026-09-21 | Decisión del founder (2026-09-21): «Coincidencias debe abrir eventos con coincidencias». Hoy la tarjeta de Administración abre la lista genérica de la semana (39 eventos, de 30 en 30). Filtro nuevo en `panel_eventos()` / `panel_fichas_conteos()` con el MISMO criterio que el número de `indicadores_ahora()` (rol de entonces incluido), enlace de la tarjeta en `Indicadores.tsx` / `panel.ts`. Migración que solo reemplaza funciones: `20260922100000_coincidencias_lista.sql` | Número y lista del mismo criterio, con banco PGlite. La migración la aplica el gestor con permiso del founder. El `.env` real nunca entra a la carpeta del operador. |
| Publicar evento abre evento / mismo chat de OL-100 «Lugar y dirección en el alta de evento» (Sonnet 5, esfuerzo medio), en rama aparte y antes de su código | `publicar-evento-sin-lugares`, base main del día | OL-103 / 138, reservados por gestor 2026-09-21 | Bug del founder (2026-09-21): «Estoy viendo Agenda/todos con ciudad de méxico activo, Al seleccionar "Publicar Evento" me abre formulario de registrar lugar». Causa medida por el gestor: `src/components/Publicar.tsx:36` manda a `/lugares/nuevo` cuando la ciudad no tiene lugares (`hayLugares`), regla de cuando el evento exigía lugar registrado. Sin migración | Arreglo mínimo con prueba; comprobar que el alta de evento funciona en una ciudad sin lugares («otro sitio»). Entrega local al gestor. |
| Botón en el renglón en vez de deslizar / chat nuevo «Botón en el renglón en vez de deslizar (OL-104)» (Sonnet 5, esfuerzo medio) | `boton-en-renglon`, base main del día | OL-104 / 139, reservados por gestor 2026-09-21 | Propuesta del founder (2026-09-21): «eliminar swipe options e ir directamente a colocar botón de "Voy/Vas" en eventos, "Seguir/Sigues" en lugares y artistas. para evitar el glitch». `RenglonEvento`, `RenglonLugar`, `RenglonArtista`, `ui/Deslizable` y quienes lo usan (agenda, listas, Mi perfil, fichas). Cambia el canon de OL-056/OL-057. Sin migración | **Prototipo firmado antes que código.** El código empieza con el PR #116 ya en main. Entrega local al gestor. El `.env` real nunca entra a la carpeta del operador. |
| Aviso al salir de la plataforma / chat nuevo «Aviso al salir de la plataforma (OL-105)» (Sonnet 5, esfuerzo medio) | `aviso-al-salir`, base main del día | OL-105 / 140, reservados por gestor 2026-09-21 | Pedido del founder (2026-09-21): «Cuando el usuario salga de la plataforma por medio de link, notificar que sale de la plataforma con un elemento emergente (puede ser bottom sheet con accionables de confirmación y checkbox de no volver a notificar) […] para los casos en donde se compren boletos o se pidan otros datos, que lo sepa explícitamente.» Un componente de enlace externo y la hoja (`ui/Hoja`); enlaces de evento (boletos), redes de artistas y lugares. Sin migración | **Prototipo firmado antes que código.** No toca «Cómo llegar» ni compartir sin que el founder lo decida en el prototipo. El `.env` real nunca entra a la carpeta del operador. |
| Botón en las tarjetas de los carriles / mismo chat de OL-104 «Botón en el renglón en vez de deslizar» (Sonnet 5, esfuerzo medio) | `boton-en-carriles`, base main del día | OL-106 / 141, reservados por gestor 2026-09-21 | Pedido del founder (2026-09-21): «Incluye botón voy y seguir en templates de destacados y con eventos en esta semana. (Agenda, lugares y artistas.» `Destacados.tsx` y su CSS (carriles de Destacados y «Con eventos esta semana»), reutilizando `ui/BotonRenglon` y los hooks de OL-104. Sin migración | **Prototipo firmado antes que código** (tarjetas chicas, las de artistas redondas). No romper `huboArrastre` ni la memoria del carril. Maquetación medida. El `.env` real nunca entra a la carpeta del operador. |
| Agendas de las instituciones por correo (D1) / chat nuevo (Sonnet 5, esfuerzo medio) | `agendas-por-correo`, solo documentos | OL-107 / 142, reservados por gestor 2026-09-21 | `docs/rediseno/31-agendas-por-correo.md`, `scripts/instituciones/` solo lectura | Nada se envía: texto y lista los firma el founder. Correos solo de fuentes públicas oficiales; la lista de correos no entra al repo público sin decisión del founder. |
| Fichas por completar (D2) / chat nuevo (Sonnet 5, esfuerzo medio) | `fichas-por-completar`, solo documentos | OL-108 / 143, reservados por gestor 2026-09-21 | `docs/rediseno/32-fichas-por-completar.md` | Producción solo lectura (conteos e ids, `begin…rollback`). Nada se publica sin el founder. Fotos: solo propuestas con su fuente y licencia. |
| Fricción en los tres formularios (D3) / chat nuevo (Sonnet 5, esfuerzo medio) | `friccion-formularios`, solo documentos y capturas | OL-109 / 144, reservados por gestor 2026-09-21 | `docs/rediseno/33-friccion-formularios.md` | Sin tocar código. Respaldo local con datos inventados; el `.env` real nunca entra a la carpeta. Capturas PNG reales. |
| Alta de lugar y de artista al canon / chat nuevo (Sonnet 5, esfuerzo medio) | `alta-lugar-artista-canon`, base main con el PR #133 | OL-110 / 145, reservados por gestor 2026-09-21 | `FormularioLugar.tsx` y su CSS, `FormularioArtista.tsx`, `FormularioPerfil.tsx`, `ui/ListaFlotante` (solo usarla) | Sin migración. Capturas PNG reales que abre el gestor. No tocar `HojaDondeEs` ni `ui/BotonRenglon` (OL-106). |
| Analítica de Vercel / chat nuevo (Haiku 4.5, esfuerzo medio) | `analitica-vercel`, base main del día | OL-111 / 146, reservados por gestor 2026-09-21 | `package.json`, `src/app/layout.tsx`, el aviso de privacidad | Solo vistas de página; sin eventos a medida; la línea del aviso la firma el founder antes de publicar. Sin migración. |
| Letrero de activar avisos según el medio / chat nuevo (Sonnet 5, esfuerzo medio) | `avisos-segun-el-medio`, base main del día | OL-112 / 147, reservados por gestor 2026-09-21 | `components/ActivarAvisos.tsx`, `perfil/AvisosPerfil.tsx`, `novedades/TelefonoAun.tsx`, `lib/push*`, textos «este teléfono» de los formularios | Causa medida antes que arreglo. Sin migración. No tocar `layout.tsx` (OL-111 trabaja ahí). |
| Cartel sin título: el nombre como faltante / chat nuevo (Sonnet 5, esfuerzo medio) | `nombre-faltante-tras-cartel`, base main del día | OL-113 / 148, reservados por gestor 2026-09-21 | `eventos/FormularioEvento.tsx` (solo el campo del nombre y su ayuda), `eventos/TarjetaCartel.tsx` si hace falta | Causa medida. Sin migración. No tocar `HojaDondeEs*`, `layout.tsx` ni `ActivarAvisos*`. |
| Identidad del artista (B2) / chat nuevo (Sonnet 5, esfuerzo medio) | `identidad-del-artista`, base main del día | OL-114 / 149, migración `20260922140000_artistas_slug.sql`, reservados por gestor 2026-09-21 | `src/app/artistas/**`, `lib/artistas*`, `FormularioArtista.tsx`, reclamo | Migración solo añade; sin subdominios; los `/artistas/<uuid>` viejos siguen funcionando. No tocar `layout.tsx`, `ui/Cabecera`, avisos. |
| Avisos al administrador (B3) / chat nuevo (Sonnet 5, esfuerzo medio) | `avisos-al-admin`, base main del día | OL-115 / 150, migración `20260922150000_avisos_admin.sql`, reservados por gestor 2026-09-21 | `ui/Cabecera*`, `lib/avisos*`, `api/avisos-pendientes`, `admin/**` (solo lectura del resumen), `sw.js` si hace falta | Ningún envío real en pruebas; revisión del gestor con más cuidado. No tocar `layout.tsx` (OL-111) ni `src/app/artistas/**` (OL-114). |
| Renglón de dato: icono alineado con el texto; icono de Administración / chat nuevo (Haiku 4.5, esfuerzo medio) | `renglon-dato-alineado`, base main del día | OL-116 / 151, reservados por gestor 2026-09-22 | `ui/Ficha.module.css` (`.dato`), el mismo patrón donde se repita, `components/Sesion.tsx` e `ui/Iconos.tsx` (solo el icono de Administración) | Sin migración. Solo el renglón y el icono; nada de rediseñar. Capturas reales 390×844 con `next build && next start`. |
| Pincel: permiso del sensor en iPhone («no tiene sensor de movimiento») / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-sensor-ios`, base main del día | OL-117 / 152, reservados por gestor 2026-09-22 | `src/app/obra/[id]/mando/Mando.tsx`, `mando.module.css`, `src/lib/pincel.ts` si hace falta | Causa medida; pedir el permiso desde un gesto que iOS acepte; sin migración. La prueba en iPhone la hace el founder. |
| Pincel: QR en la pared y en Administración / chat nuevo (Sonnet 5, esfuerzo medio) | `pincel-qr`, base main del día | OL-118 / 153, reservados por gestor 2026-09-22 | `package.json` (+`qrcode`, decidido por el founder), `src/lib/qr.ts`, `obra/[id]/pared/*`, `admin/obras-colectivas/[id]/*` | Sin migración. QR generado en el servidor como SVG; capturas reales 1280×800, 1920×1080 y 390×844; QR decodificado. No tocar `Mando.tsx` (OL-117). |
| Direcciones legibles para lugares y eventos / chat nuevo (Sonnet 5, esfuerzo medio) | `slugs-lugares-eventos`, base main del día | OL-119 / 154, migraciones `20260922160000_lugares_slug.sql` y `20260922170000_eventos_slug.sql`, reservados por gestor 2026-09-22 | `lugares/[id]`, `eventos/[id]`, enlaces internos con UUID, `sitemap.ts`, `lib/avisos*` | Patrón de OL-114; migraciones solo añaden y las aplica el gestor; obras fuera. No tocar `src/app/obra/**` (OL-117/118) ni `ui/Ficha.module.css` (OL-116). |
| Pincel: ajustes tras la prueba del founder (eje invertido, punto de posición, botón Centrar, QR chico arriba a la derecha) / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-ajustes-prueba`, base main del día | OL-120 / 155, reservados por gestor 2026-09-22 | `src/app/obra/**`, `src/lib/pincel.ts` | Sin migración. Nada se mueve en la rejilla del mando; capturas reales. |
| Pincel: tope global (2 obras, 40 mandos) e interruptor «Pincel apagado» / chat nuevo (Sonnet 5, esfuerzo medio) | `pincel-freno`, base main del día | OL-121 / 156, migración `20260922180000_pincel_freno.sql`, reservados por gestor 2026-09-22 | `admin/obras-colectivas/**`, tabla `ajustes_sitio`, políticas de `realtime.messages`, pantalla «apagado» en pared y mando | Migración solo añade y la aplica el gestor; banco SQL obligatorio. No tocar `Mando.tsx`/`Pared.tsx` más allá de la pantalla de apagado (OL-120). El freno del cortafuegos `/obra/*` lo pone el gestor con el founder. |
| Envío del correo a instituciones (D1, pieza «envío») / chat nuevo (Sonnet 5, esfuerzo medio) | `correo-instituciones-envio`, base main del día | OL-122 / 157, migración `20260922190000_agendas_invitaciones.sql`, reservados por gestor 2026-09-22 | `scripts/instituciones/invitar-agendas.ts`, `scripts/instituciones/agendas.json` (sin correos), tabla `agendas_invitaciones_enviadas` | Ensayo por defecto; ningún envío sin orden del founder; correos y teléfono solo fuera del repo. |
| Redirección 308 real desde UUID y cierre de concurrencia en el slug de artistas / chat nuevo (Sonnet 5, esfuerzo medio) | `redireccion-308-slugs`, base main del día | OL-123 / 158, migración `20260922200000_artistas_slug_lock.sql`, reservados por gestor 2026-09-22 | `src/proxy.ts` o las tres páginas `[id]`, `artistas_generar_slug` | Migración solo `create or replace` de la función; sin tocar `src/app/obra/**`. |
| Mapa de lugares (B1), prototipo / chat nuevo (Sonnet 5, esfuerzo medio) | `mapa-lugares-prototipo`, base main del día | OL-124 / 159, reservados por gestor 2026-09-22 | `docs/rediseno/35-mapa-de-lugares.md`, `docs/rediseno/prototipos/mapa-lugares.html` | Solo prototipo y doc; sin código de la app hasta la firma del founder. |
| Mapa de lugares en la app (B1, código sobre el doc 35 firmado) / chat nuevo (Sonnet 5, esfuerzo medio) | `mapa-lugares-app`, base main del día | OL-125 / 160, migración solo si hace falta `20260922210000_mapa_lugares_semana.sql`, reservados por gestor 2026-09-22 | `src/app/lugares/**`, `lib/geo.ts`, `ui/Hoja` (solo usarla) | Calcar el prototipo firmado; «Cercanos» fuera de las pestañas del mapa; capturas reales de los tres estados. No tocar `src/app/obra/**` ni `ui/Ficha.module.css`. |
| Pincel: grosor en el iPhone real y latencia (segunda prueba del founder) / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-grosor-y-latencia`, base main del día | OL-126 / 161, reservados por gestor 2026-09-22 | `src/app/obra/**`, `src/lib/pincel.ts` | Sonda detrás de `?sonda=1`; presupuesto de Realtime respetado; la prueba final la hace el founder en su iPhone. Sin migración. |
| Pincel: cercanía del mando y crear pared en la ubicación actual / operador de Pincel (`local_23fc79ff`, Sonnet 5), después de OL-126 | `pincel-cercania`, base main del día | OL-127 / 162, migración `20260922230000_obras_ubicacion.sql` si hace falta, reservados por gestor 2026-09-22 | `src/app/obra/**`, `admin/obras-colectivas/**`, `lib/geo.ts` | Radio 200 m propuesto, a confirmar por el founder; administración exenta. |
| Mapa: pines (día siempre visible, pin compacto, tres tamaños, color de seguidos) / operador del mapa (`local_0eedcf02`, Sonnet 5) | `mapa-pines`, base main del día | OL-128 / 163, reservados por gestor 2026-09-22 | `src/components/Mapa.tsx`, `src/app/lugares/**`, doc 35 y prototipo | Sin migración. Capturas con Mapbox real a tres zooms. |
| Avisos en la computadora: causa del fallo al activar / chat nuevo (Sonnet 5, esfuerzo medio) | `avisos-escritorio-causa`, base main del día | OL-129 / 164, reservados por gestor 2026-09-22 | `components/ActivarAvisos.tsx`, `lib/pushCliente.ts`, `lib/plataforma.ts`, `public/sw.js`, acción de la suscripción | Causa medida; el letrero dice el motivo; sin envíos reales. No tocar `obra/**`, `lugares/**`, `Mapa.tsx`. |
| Pincel: el título de la obra sigue al lugar elegido hasta que se edita a mano / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-titulo-obra`, base main del día | OL-130 / 165, reservados por gestor 2026-09-22 | `admin/obras-colectivas/CrearObraAqui.tsx` | Sin migración. |
| Pincel: desplazamiento bloqueado y grosor en vivo mientras se pinta; latencia (tercera prueba) / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-grosor-en-vivo`, base main del día | OL-132 / 167, reservados por gestor 2026-09-22 | `src/app/obra/**`, `src/lib/pincel.ts` | Sin migración; sonda con ping de ida y vuelta; la lectura de latencia real la da el founder. |
| Avisos en la computadora: interruptor de Ajustes deshabilitado en Chrome de Mac / operador de avisos (`local_01172234`, Sonnet 5) | `avisos-escritorio-ajustes`, base main del día | OL-131 / 166, reservados por gestor 2026-09-22 | `perfil/AvisosPerfil.tsx`, `lib/plataforma.ts`, `lib/pushCliente.ts` | Causa medida; la suscripción se guarda para la cuenta actual; sin envíos reales. |
| Icono de Administración: herramientas en los dos accesos (cabecera y fila de Ajustes) / operador de renglones (`local_6e668930`, Sonnet 5) | `icono-admin-herramientas`, base main del día | OL-133 / 168, reservados por gestor 2026-09-22 | `components/ui/Iconos.tsx`, `components/Sesion.tsx`, `app/ajustes/page.tsx` | Sin migración; el escudo se queda para privacidad y rol. |
| Pincel: «Borrar la pared» no borra (bug del founder en producción) / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-borrar-fiable`, base main del día | OL-134 / 169, reservados por gestor 2026-09-22 | `admin/obras-colectivas/acciones.ts`, `admin/obras-colectivas/[id]/BorrarPared.tsx`, `obra/[id]/pared/Pared.tsx`, `lib/pincel.ts` | Sin migración; el servidor borra la instantánea; la pared no depende solo del broadcast. |
| Pincel: la pared mantiene su proporción y solo se escala (no se deforma en pantallas verticales) / operador de Pincel (`local_23fc79ff`, Sonnet 5), después de OL-134 | `pared-proporcion`, base main del día | OL-135 / 170, reservados por gestor 2026-09-22 | `obra/[id]/pared/Pared.tsx`, `pared.module.css`, `lib/pincel.ts` | Sin migración; proporción fija de la pared, lienzo centrado con márgenes; instantánea y posiciones en esa misma proporción. |
| Pincel: llegada de un mando nuevo («Entrando…» en el celular, el punto crece y vuelve en la pared, letrero «Nombre entró» abajo a la izquierda) / operador de Pincel (`local_23fc79ff`, Sonnet 5) | `pincel-llegada`, base main del día | OL-136 / 171, reservados por gestor 2026-09-22 | `obra/[id]/mando/Mando.tsx`, `obra/[id]/pared/Pared.tsx`, `pared.module.css`, `mando.module.css`, `lib/pincel.ts`, `prototipos/pincel-llegada.html` | Sin migración; prototipo HTML primero (captura al founder), código en la misma pieza; presencia lleva el nombre. |
| E1 · Buscar lugar en el alta de evento (URGENTE) / operador de renglones (`local_6e668930`, Sonnet 5, esfuerzo medio) | `alta-evento-buscar-lugar`, base main del día | OL-137 / 172, reservados por gestor 2026-09-23 | `docs/rediseno/prototipos/alta-evento-lugar.html`, `src/app/eventos/HojaDondeEs.tsx`, `FormularioEvento.tsx`, `SelectorQuien.tsx` y sus CSS; bitácora 172 y `capturas-172/` |
| Inicio, segunda vuelta / chat nuevo «Inicio, segunda vuelta (OL-156)» (Sonnet 5, esfuerzo medio) | `inicio-segunda`, base main del día | OL-156 / 191, reservados por gestor 2026-09-23 | `src/app/page.tsx` → `/agenda`, `src/app/inicio/`, `src/app/manifest.ts`, `src/proxy.ts` (redirección), `AgendaInicio.tsx`, `Destacados.tsx`, `lib/inicio.ts`; bitácora 191 y `capturas-191/` |
| Inicio en la app, código / chat nuevo «Inicio en la app (OL-153)» (Sonnet 5, esfuerzo medio) | `inicio-app`, base main del día | OL-153 / 188, reservados por gestor 2026-09-23 | `src/app/inicio/`, `src/components/Destacados.tsx` y CSS (tarjeta de carril con tres tamaños), barra inferior (`ui/Barra`), `ui/Cabecera`, buscador; bitácora 188 y `capturas-188/` |
| Perfil del artista: Mis artistas, compartir y video / chat nuevo «Perfil del artista en la app (OL-154)» (Sonnet 5, esfuerzo medio) | `perfil-artista-app`, base main del día | OL-154 / 189, reservados por gestor 2026-09-23 | `src/app/perfil/`, `src/app/artistas/[id]/`, `ui/CodigoQr`; bitácora 189 y `capturas-189/` |
| C1 · Perfil del artista como enlace único, propuesta / chat nuevo (Sonnet 5, esfuerzo medio) | `perfil-artista-enlace`, solo documento y prototipo | OL-149 / 184, reservados por gestor 2026-09-23 | `docs/rediseno/40-perfil-artista-enlace.md`, `prototipos/perfil-artista-enlace.html`, bitácora 184 y `capturas-184/` |
| C2 + C4 · Inicio personalizado y buscador único, prototipo / chat nuevo (Sonnet 5, esfuerzo medio) | `inicio-personalizado`, solo documento y prototipo | OL-150 / 185, reservados por gestor 2026-09-23 | `docs/rediseno/41-inicio-personalizado.md`, `prototipos/inicio-personalizado.html`, bitácora 185 y `capturas-185/` |
| C5 · Festivales y eventos con varios actos, propuesta / chat nuevo (Sonnet 5, esfuerzo medio) | `festivales-modelo`, solo documento | OL-151 / 186, reservados por gestor 2026-09-23 | `docs/rediseno/42-festivales.md`, bitácora 186 |
| E9 · Ajustes de cargador y transiciones / chat nuevo, al final (Sonnet 5, esfuerzo medio) | `transiciones-ajustes`, base main del día | OL-152 / 187, reservados por gestor 2026-09-23 | `src/app/template.tsx`, `AgendaInicio.tsx`, `loading.tsx` de agenda; bitácora 187 y `capturas-187/` |
| Transiciones y cargador, código / chat nuevo «Transiciones en la app (OL-148)» (Sonnet 5, esfuerzo medio) | `transiciones-app`, base main del día | OL-148 / 183, reservados por gestor 2026-09-23 | `src/app/**/template.tsx`, `loading.tsx`, `globals.css`, `ui/Pestanas`; bitácora 183 y `capturas-183/` |
| Texto largo a pantalla completa, código / chat nuevo «Texto largo en la app (OL-147)» (Sonnet 5, esfuerzo medio) | `texto-largo-app`, base main del día | OL-147 / 182, reservados por gestor 2026-09-23 | `src/components/ui/Campo.tsx` y CSS, `FormularioEvento.tsx`, `FormularioLugar.tsx`, `FormularioArtista.tsx`; bitácora 182 y `capturas-182/` |
| Mapa sin aros y violeta, código / chat nuevo «Mapa sin aros y violeta (OL-146)» (Sonnet 5, esfuerzo medio) | `mapa-violeta`, base main del día | OL-146 / 181, reservados por gestor 2026-09-23 | `src/app/globals.css`, `src/components/Mapa.tsx`, `src/app/lugares/VistaLugares.tsx` y CSS, `docs/diseno/LINEA_GRAFICA.md`; bitácora 181 y `capturas-181/` |
| E6 código · Indexación, nueve cambios / chat nuevo «Indexación: cambios de código (OL-143)» (Sonnet 5, esfuerzo medio) | `indexar-codigo`, base main del día | OL-143 / 178, reservados por gestor 2026-09-23 | `src/app/{lugares,artistas,eventos}/[id]/page.tsx` (metadatos, JSON-LD), páginas `nuevo`/`editar`, `novedades`, `layout.tsx`; bitácora 178 |
| B5 · Transiciones y cargador / chat nuevo «Transiciones y cargador (OL-144)» (Sonnet 5, esfuerzo medio) | `transiciones-cargador`, solo prototipo | OL-144 / 179, reservados por gestor 2026-09-23 | `docs/rediseno/prototipos/transiciones.html`, `docs/rediseno/38-transiciones-cargador.md`, bitácora 179 y `capturas-179/` |
| B6 · Texto largo a pantalla completa / chat nuevo «Texto largo a pantalla completa (OL-145)» (Sonnet 5, esfuerzo medio) | `texto-largo`, solo prototipo | OL-145 / 180, reservados por gestor 2026-09-23 | `docs/rediseno/prototipos/texto-largo.html`, `docs/rediseno/39-texto-largo.md`, bitácora 180 y `capturas-180/` |
| E5 · Mapa con menos ruido y color primario / operador de Pincel (`local_23fc79ff`, Sonnet 5, esfuerzo medio) | `mapa-ruido-color`, solo prototipo y documento | OL-141 / 176, reservados por gestor 2026-09-23 | `docs/rediseno/prototipos/mapa-lugares.html`, `docs/rediseno/37-color-primario.md`, bitácora 176 y `capturas-176/` |
| E6 · Indexar el catálogo / operador de avisos (`local_01172234`, Sonnet 5, esfuerzo bajo) | `indexar-catalogo`, solo documento | OL-142 / 177, reservados por gestor 2026-09-23 | `docs/rediseno/36-indexar-catalogo.md`, bitácora 177 |
| E4 · «Cooperación solidaria» en el costo / operador de renglones (`local_6e668930`, Sonnet 5, esfuerzo bajo) | `costo-cooperacion`, base main del día | OL-140 / 175, reservados por gestor 2026-09-23 | `src/app/eventos/FormularioEvento.tsx`, ficha de evento, compartir, migración si hace falta; bitácora 175 y `capturas-175/` |
| E7 · Texto al salir de la app / operador de Pincel (`local_23fc79ff`, Sonnet 5, esfuerzo bajo) | `texto-al-salir`, base main del día | OL-139 / 174, reservados por gestor 2026-09-23 | `src/components/ui/EnlaceExterno.tsx` y su prueba; bitácora 174 y `capturas-174/` |
| B4 + E2 · Textos de avisos y correos con boletín quincenal / operador de avisos (`local_01172234`, Sonnet 5, esfuerzo medio) | `avisos-textos-boletin`, solo documentos | OL-138 / 173, reservados por gestor 2026-09-23 | `docs/rediseno/28-avisos-y-boletin.md`, bitácora 173 |
| Limpieza: carga de «Nuevos» huérfana / el propio gestor en la nube (`session_01XrJvzKysk1Y39vzpLGT9HG`) | `limpieza-nuevos-huerfanos`, base main del día | OL-166 / 201, reservados por gestor 2026-09-24 | `src/lib/cargarNuevos.ts`, `src/lib/nuevosVisto.ts` (+ prueba), acción `cargarNuevos` de `src/app/accionesAgenda.ts` y su prueba; comentario de `lib/cargarCercanos.ts` | Solo borrar lo que ninguna pantalla usa; `cargarCercanos` no se toca. Sin migración. Orden del founder «une los PRs y limpia». |
| F1 · Botón de compartir sin envolvente y reparto de enlaces a partir de 3 / operador nuevo en la nube (Sonnet 5, esfuerzo bajo) | `ficha-boton-compartir-y-reparto`, base main del día | OL-167 / 202, reservados por gestor 2026-09-24 | `src/components/ui/Ficha.module.css` (`.accion`), `src/lib/ficha.ts` y su prueba, las tres fichas solo si hace falta el conteo; bitácora 202 y `capturas-202/` | En producción (PR #202). Causa medida por el gestor: `.accion` nació para `<a>` y en el `<button>` de `BotonCompartir` queda el borde y fondo nativos. Con 1 o 2 acciones, a la izquierda; 3 y 4 repartidas; más, carril. Sin migración. Capturas reales 390×844 con datos al tope. |
| F5 · Mixcloud reconocido y título editable de cada enlace / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `enlaces-titulo-y-redes`, base main del día | OL-168 / 203, reservados por gestor 2026-09-24 | `src/lib/enlaces.ts` y prueba, `ui/IconoRed.tsx`, `FormularioArtista.tsx`, `FormularioLugar.tsx`, validación en servidor de `redes` (jsonb, campo `titulo` opcional), fichas de artista y lugar (etiqueta) | En producción (PR #206, tres vueltas de revisión). Sin migración si `redes` sigue siendo jsonb sin restricción de forma; si hiciera falta, solo añade y la aplica el gestor. Tope del título 30 caracteres. Capturas reales 390×844 del formulario y de la ficha. |
| Arreglo de CI: canal falso de Pincel en secuencia / el propio gestor en la nube | `pincel-prueba-canal-orden`, base main del día | OL-172 / 207, reservados por gestor 2026-09-24 | `scripts/pincel/simulador-mandos.test.ts` (solo el ayudante `canalFalso`) | Sin tocar el código de Pincel ni ninguna afirmación de las pruebas. |
| F2 código · Lugar del evento a pantalla completa en la app / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `lugar-evento-pantalla-app`, base main del día | OL-173 / 208, reservados por gestor 2026-09-24 | `src/app/eventos/HojaDondeEs.tsx` y CSS (se reconstruye), `FormularioEvento.tsx` (solo el renglón «Dónde»), `src/app/lugares/acciones.ts` (alta automática desde el evento), `src/lib/buscarLugares.ts`, `geocodificar.ts`, `direccionContexto.ts`, `ui/Hoja` (modo pantalla completa), `Mapa.tsx` si hace falta; bitácora 208 y `capturas-208/` | Calca el doc 43 y su prototipo con la variante **B** por defecto (founder sin firma expresa el 2026-09-24: «no veo ajustes a agregar lugar»); la variante A queda a un cambio chico. Sin migración salvo que el alta automática exija una columna «privado» (solo añade; la aplica el founder o el gestor local). Sugerencias siempre bajo el campo en uso. No tocar `ui/Cabecera`, `AgendaInicio`, `VistaLugares` (OL-174) ni `src/app/artistas/**` (OL-175). |
| F3 código · Cabecera de Lugares con chip de fecha; chip solo icono; «Ver en lista» abajo / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `lugares-cabecera-fecha-app`, base main del día | OL-174 / 209, reservados por gestor 2026-09-24 | `src/components/AgendaInicio.tsx` (chip), `src/components/ui/Chip*`, `ui/SelectorFecha*`, `ui/Cabecera*`, `src/app/lugares/VistaLugares.tsx` y CSS, `src/components/Mapa.tsx` (filtro por día), `src/lib/lugares.ts`; bitácora 209 y `capturas-209/` | Calca el doc 45: fecha corta siempre («mié 30 sep»), chip inicial solo icono, filtro por fecha en el mapa (la lista sigue igual salvo que el founder diga otra cosa), «Ver en lista»/«Ver en mapa» flotante sobre «Registrar lugar», chip de ciudad que se encoge y corta antes que la lupa. Sin migración. No tocar `src/app/eventos/**` (OL-173) ni `src/app/artistas/**` (OL-175). |
| F4 fase 1 · Novedades del artista: modelo, migración y alta con YouTube / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `novedades-artista-fase1`, base main del día | OL-175 / 210, migración `20260925100000_novedades_artista.sql`, reservados por gestor 2026-09-24 | Migración nueva (tabla `novedades_artista`, RLS con `gestiona_artista`, tope 5/día), `src/app/artistas/[id]/**` (sección Novedades con `VideoEmbed` para YouTube), `src/app/perfil/**` o `artistas/[id]/novedades/` (pantalla «Publicar novedad»), `src/lib/novedades.ts` y pruebas con el banco PGlite; bitácora 210 y `capturas-210/` | Fase 1 del doc 44 tal cual: un proveedor (YouTube), sin push todavía (fase 3), sin más proveedores (fase 2). Migración solo añade y la aplica el founder o el gestor local ANTES de publicar; el PR lo dice. No tocar `lib/enlaces.ts`, `src/app/eventos/**` ni `ui/Cabecera*`. |
| F6 · Chip «Te interesa» en las tarjetas de los carriles / operador nuevo en la nube (Sonnet 5, esfuerzo bajo) | `carril-chip-te-interesa`, base main del día | OL-176 / 211, reservados por gestor 2026-09-24 | `src/components/Destacados.tsx` y CSS, `src/lib/destacados.ts` (tipo `Tarjeta`) y `useAsistenciaEnLista.tsx` solo si el estado no llega ya con el botón; pruebas; bitácora 211 y `capturas-211/` | Pedido del founder (2026-09-24): «Usas un chip en listado de eventos para decir “Te interesa”, por favor úsalo también en los elementos de sliders. Se puede apilar en el extremo inferior izquierdo con chip de los que “van”.» Sin migración. No tocar `AgendaInicio.tsx`, `ui/Cabecera*` (OL-174), `src/app/eventos/**` (OL-173) ni `src/app/artistas/**` (OL-175). |
| F7 · «Mis artistas» arriba del listado y letrero «tu correo está enlazado a…» con reclamo directo / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `artistas-mis-fichas-y-reclamo`, base main del día | OL-177 / 212, migración `20260925110000_artistas_con_mi_correo.sql` (solo una función de lectura), reservados por gestor 2026-09-24 | `src/app/artistas/page.tsx`, `src/components/ListaArtistas.tsx`, componente nuevo `src/components/ReclamarPorCorreo.tsx`, `src/app/artistas/acciones.ts` (solo añadir), reutilizar `src/app/perfil/MisArtistas.tsx` sin editarlo; bitácora 212 y `capturas-212/` | Pedido del founder (2026-09-24). Reutiliza `reclamarArtista` (aprobación automática con correo coincidente y aviso al admin, OL-114/115) y el letrero de invitación de `Inicio.tsx`. La migración la aplica el founder o el gestor local antes de publicar. No tocar `src/app/artistas/[id]/**` ni `src/app/perfil/**` (OL-175), ni `AgendaInicio`/`Cabecera` (OL-174), ni `eventos/**` (OL-173), ni `Destacados` (OL-176). |
| Revisión de seguridad: URLs que mete la gente y el resto del desarrollo / operador nuevo en la nube (Sonnet 5, esfuerzo alto) | `revision-seguridad`, solo documentos | OL-178 / 213, reservados por gestor 2026-09-24 | `docs/rediseno/46-revision-seguridad.md`, bitácora 213; lectura de todo `src/`, `supabase/`, `public/sw.js`, `next.config.ts`, `vercel.json` | Pedido del founder (2026-09-24): «Revisa posibles brechas de seguridad en todos los casos donde permitimos a los usuarios inyectar url, corre una revisión del desarrollo para identificar otras brechas de seguridad». Sin cambios de código: hallazgos con evidencia, gravedad y arreglo propuesto; las correcciones se encargan como piezas aparte con el OK del founder. Sin acceso a producción ni a secretos. |
| F9 · Lugar privado como registro real y un solo botón «Agregar» / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `lugares-privados`, base `origin/lugar-evento-pantalla-app` (OL-173, PR #212); se une después de #212 | OL-179 / 214, migración `20260925120000_lugares_privados_de_todos.sql`, reservados por gestor 2026-09-24; **en main (PR #217, 2026-09-24)**, el operador cerró su árbol | `supabase/migrations` (columna `privado`, políticas y funciones que listan lugares), `src/app/eventos/HojaDondeEs*` (barra de un botón, panel), `src/app/lugares/acciones.ts`, `src/lib/buscarLugares.ts`, listados/mapa/buscador/sitemap/JSON-LD de lugares (filtro explícito `privado = false`), ficha de lugar (`noindex` si es privado); bitácora 214 y `capturas-214/` | Decisión del founder 2026-09-24 (Decidido). El evento con lugar privado se guarda como hoy «reservado» (sin `lugar_id`), así ningún consumidor de eventos cambia. La migración la aplica el founder o el gestor local antes de publicar. No tocar `Cabecera*`, `AgendaInicio`, `artistas/**`, `perfil/**`. |
| Remediaciones de seguridad S-01 a S-06 / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `seguridad-remediaciones`, base main del día | OL-180 / 215, reservados por gestor 2026-09-24; **en main (PR #216, 2026-09-24)**, el operador cerró su árbol | `src/lib/artistas.ts`, `lugares.ts`, `eventos.ts` (validación de imagen), acciones de artistas/lugares/eventos (rol admin en servidor), `src/lib/enlaces.ts` (homógrafos, usuario incrustado, caracteres invisibles), `src/app/api/avisos-pendientes/route.ts` y `recordatorios/route.ts` (comparación en tiempo constante), `src/lib/eventos.ts` (enlace de boletos bien formado); pruebas; bitácora 215 | «adelante para remediaciones de seguridad» (founder, 2026-09-24). Solo los hallazgos S-01 a S-06 del doc 46; sin migración; sin cambiar comportamiento visible salvo rechazar lo inválido con ayuda bajo el campo. |
| Novedades del artista, fase 2: Vimeo, SoundCloud, Bandcamp y Mixcloud / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `novedades-proveedores`, base main del día | OL-181 / 216, migración `20260925130000_novedades_proveedores.sql` (amplía la lista blanca de `proveedor` y añade `embed_id` para Bandcamp), reservados por gestor 2026-09-24; **en main (PR #220)**, árbol cerrado | `src/lib/novedadesArtista.ts`, nuevo `src/lib/incrustado.ts`, nuevo `ui/Incrustado.tsx`, `src/app/artistas/[id]/SeccionNovedades.tsx`, `page.tsx`, `novedades/acciones.ts` (oEmbed de Bandcamp al publicar, solo a `*.bandcamp.com`), `novedades/nueva/FormularioNovedad.tsx`; pruebas; bitácora 216 y `capturas-216/` | Founder 2026-09-24: «necesito que funcione con bandcamp y soundcloud, además de vimeo». Doc 44 §2 y §3 al pie de la letra (src por regex, lista blanca de dominios, sandbox por proveedor). No tocar `lib/video.ts` ni `ui/VideoEmbed` (OL-154). La red a los proveedores está cerrada en la nube: capturas con marcos vacíos, la reproducción la prueba el founder. |
| «¿Dónde es?»: agregar lugar sin punto inventado, lista que no tapa la barra / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `donde-es-agregar-ubicacion`, base main del día; prototipo primero (parte 1, termina el turno), código después (parte 2, con el visto bueno del gestor) | OL-182 / 217, sin migración, reservados por gestor 2026-09-24; **en main (PR #223)**, árbol cerrado | `docs/rediseno/prototipos/lugar-evento-pantalla.html` (o `lugar-evento-agregar.html`), doc 43 (sección «segunda versión»), `src/app/eventos/HojaDondeEs*`, `dondeEsPantalla.ts`, `MapaDondeEs.tsx`, `ui/ListaFlotante.tsx` (prop `reservaAbajo`); bitácora 217 y `capturas-217/` | Defectos del founder en producción (2026-09-24): lista sobre el botón «Agregar»; el panel guardaba un punto inventado (`moverPin(contexto.centro)`). Firmado por el founder: opción 1 (hoja a media pantalla, campo Dirección con sugerencias, «Guardar» apagado hasta fijar el punto). No tocar `src/app/artistas/**` (OL-181). |
| Letrero QR del artista con «Atrás» e «Imprimir o guardar PDF» / operador nuevo en la nube (Sonnet 5, esfuerzo bajo) | `letrero-atras`, base main del día | OL-183 / 218, sin migración, reservados por gestor 2026-09-24; **en main (PR #219)**, árbol cerrado | nuevo `src/app/artistas/[id]/letrero/BarraLetrero.tsx`, `letrero.module.css`, `letrero/page.tsx` (texto de ayuda); bitácora 218 y `capturas-218/` | Defecto del founder en la app instalada (2026-09-24): «abre el documento en app pero no hay botón para cerrar o salir». La página del letrero no tenía salida sin barra del navegador. Barra no imprimible; `CompartirFicha` no se toca. No tocar la ficha ni `novedades/**` (OL-181) ni `eventos/**` (OL-182). |
| Reordenar enlaces con arrastre en el selector de enlaces (artista y lugar) / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `enlaces-reordenar`, base main del día | OL-184 / 219, sin migración, reservados por gestor 2026-09-24; **en main (PR #222)**, árbol cerrado | `src/components/SelectorEnlaces.tsx` (+css), `ui/Iconos.tsx` (`IconoAgarre`), nuevo `src/lib/reordenar.ts` (+test); bitácora 219 y `capturas-219/` | Founder 2026-09-24: «Activa reorganización de enlaces en perfil de artista con drag and drop». Arrastre con eventos de puntero (Safari iOS), sin dependencia nueva; flechas en el agarre para teclado/VoiceOver. El componente es compartido, así que el lugar también lo gana. No tocar `novedades/**` (OL-181) ni `eventos/**`/`ListaFlotante` (OL-182). |
| Editar y borrar novedades del artista / operador nuevo en la nube (Sonnet 5, esfuerzo medio) | `novedades-editar`, base main del día | OL-185 / 220, migración `20260925140000_novedades_editar.sql` (política de update para quien gestiona; trigger que reserva `visible` a la administración y fija `artista_id`/`publicado_por`/`creado_en`), reservados por gestor 2026-09-24; **en main (PR #225)**, árbol cerrado | `src/app/artistas/[id]/novedades/acciones.ts` (+test), nueva ruta `novedades/[novedadId]/editar/page.tsx`, `novedades/nueva/FormularioNovedad.tsx` (props `inicial`, `modo`), `SeccionNovedades.tsx` («Editar» por novedad, «Oculta»), `page.tsx` de la ficha; prueba de banco; bitácora 220 y `capturas-220/` | Founder 2026-09-24: «crea opción de editar publicaciones de artista, para borrar o corregir subidas». Cambia el doc 44 §6 («sin edición»). Reutiliza `ui/Borrar`. La migración la aplica el founder. |
| F2 · Lugar del evento a pantalla completa: mapa, un solo campo, sugerencias flotantes, registro automático / operador nuevo (Sonnet 5, esfuerzo medio) | `lugar-evento-pantalla-completa`, solo prototipo primero | OL-169 / 204, reservados por gestor 2026-09-24 | `docs/rediseno/prototipos/lugar-evento-pantalla.html` (dos variantes), `docs/rediseno/43-lugar-evento-pantalla.md`; el código (`HojaDondeEs`, `FormularioEvento`, `lugares/acciones`) solo tras la firma | En main (PR #203, 2026-09-24), decisiones del founder pendientes. Prototipo con las dos variantes de las acciones «Agregar lugar» y «Buscar en el mapa sin agregar» (en la lista de resultados o como barra pegada sobre el teclado / al pie). |
| F3 · Cabecera de Lugares con chip de fecha; chip solo icono; «ver en lista» abajo / operador nuevo (Sonnet 5, esfuerzo medio) | `lugares-cabecera-fecha`, prototipo primero sobre `cabeceras.html` y `mapa-lugares.html` | OL-170 / 205, reservados por gestor 2026-09-24 | `docs/rediseno/prototipos/cabeceras.html`, `mapa-lugares.html`, doc 35; después `AgendaInicio.tsx` (chip), `VistaLugares.tsx`, `ui/Cabecera` | En main (PR #205, 2026-09-24), decisiones del founder pendientes. Medir el choque de chips anchos (ciudad larga) con el buscador. |
| F4 · Novedades del artista con reproductor incrustado y aviso a seguidores / operador nuevo (Sonnet 5, esfuerzo medio) | `novedades-artista`, propuesta y prototipo primero | OL-171 / 206, migración reservada `20260925100000_novedades_artista.sql`, reservados por gestor 2026-09-24 | `docs/rediseno/44-novedades-artista.md`, `prototipos/novedades-perfil-alta.html` (ya existe, doc 40); después tabla nueva, `lib/video.ts`/`VideoEmbed`, ficha de artista, `lib/avisos*` | En main (PR #204, 2026-09-24), decisiones del founder pendientes. Sin envíos reales en pruebas; la migración la aplica el gestor. |
| Lugar desde flyer: tras confirmar la dirección, «Listo» bloqueado y sin pin / operador nuevo local (Sonnet 5, esfuerzo medio) | `flyer-confirmar-pin`, base main del día | OL-187 / 221, sin migración, reservados por gestor 2026-09-25 | `src/app/eventos/HojaDondeEs.tsx`, `direccionEvento.ts`, `direccionContexto.ts`, `dondeEsPantalla.ts`, `src/app/lugares/HojaDonde.tsx` y sus pruebas; bitácora 221 y `capturas-221/` | Bug del founder (2026-09-25). No tocar `ui/ChipFecha`/`SelectorFecha` (OL-188). |
| Selector de fecha: elegir «hoy» no filtra / operador nuevo local (Sonnet 5, esfuerzo bajo) | `fecha-hoy-filtra`, base main del día | OL-188 / 222, sin migración, reservados por gestor 2026-09-25 | `src/components/ui/ChipFecha.tsx`, `ui/SelectorFecha.tsx`, `lib/calendario.ts`, `src/app/lugares/VistaLugares.tsx`, `src/app/eventos/SelectorCuando.tsx`, `components/AgendaInicio.tsx` y pruebas; bitácora 222 y `capturas-222/` | Bug del founder (2026-09-25). No tocar `eventos/HojaDondeEs*` (OL-187). |
| Retirar `flyer.componentes.test.mjs` (prueba de la hoja vieja de «¿Dónde es?») / operador nuevo local (esfuerzo bajo) | `retirar-prueba-flyer-vieja`, base main del día | OL-189 / 223, sin migración, reservados por gestor 2026-09-25 | solo `src/app/eventos/flyer.componentes.test.mjs` (borrado), bitácora 223 y la línea OL-189 de OPEN_LOOPS | No tocar los otros `*.componentes.test.mjs` ni `HojaDondeEs*`. Sin capturas: no hay UI. |
| Medir acciones con eventos propios de Vercel Analytics, primera entrega / chat del founder «Programa de taggeo y eventos» (modelo a elección del founder; se recomienda Sonnet 5) | `medir-acciones`, base main del día | OL-190 / 224, sin migración, reservados por gestor 2026-09-25 | nuevo `src/lib/medir.ts` (+ pruebas), llamadas dentro de las acciones de servidor existentes (auth/callback, eventos, lugares, asistencia, búsqueda) y en cliente (entrar, compartir, instalación), `src/app/privacidad/page.tsx`; bitácora 224 y `capturas-224/` | Solo después de que la acción salió bien; nunca bloquea; nunca datos personales ni texto libre; máximo 2 datos por evento; salta admins también en cliente. No tocar `AnalyticsVercel.tsx`/`limpiarUrlAnalitica.ts` salvo necesidad justificada. Merge solo con el «publica» del founder en el chat del gestor. |
| App de iOS en la tienda: documento del plan / operador nuevo local (Haiku 4.5, esfuerzo bajo) | `app-ios-plan`, base main del día | OL-191 / 225, sin migración, reservados por gestor 2026-09-25 | nuevo `docs/rediseno/47-app-ios.md`, bitácora 225, línea OL-191 de OPEN_LOOPS | Solo documentos; nada en la cuenta de Apple ni en el repo de código. Datos privados de la cuenta (tarjeta, teléfono, dirección) nunca en el repo. |

Los worktrees correspondientes son `/Users/apple-1/somosnosotros-pincel-prototipo`,
`/Users/apple-1/somosnosotros-directorios-alfabeticos` y
`/Users/apple-1/somosnosotros-avisos-operacion`. No cambiar de rama en la carpeta
de otro operador. `main` queda al gestor para coordinacion documental e integracion.
Modelos operativos: Terra; pruebas focalizadas segun riesgo. No abrir mas agentes
ni repetir suites como parte de esta asignacion.

Avisos entrego `f6c78d2` en bit113 antes de recibir la reserva formal OL-086/120.
Se conserva ese commit y esa bitacora: bit120 sera cierre operativo con referencia
a la evidencia anterior, no renumeracion ni repeticion de activacion. El operador
reporta ventana remota liberada y cron activo; revision final del gestor pendiente.


Gestor desde el 2026-09-19: la sesión Claude `c45c6c9f-46b4-426d-a884-9ddb0b295fa7` retoma la gestión tras Codex, con las mismas reglas de este registro. Operadores Claude en modelos baratos (Sonnet 5 o Haiku 4.5) y esfuerzo medio o bajo; revisión con agentes solo en migraciones, seguridad y avisos. PR105 y Pincel siguen retenidos a la espera del VoBo del founder.

## Reglas de entrega

- Una pieza por rama y OL; otra pieza o ampliacion requiere asignacion del gestor.
  La activacion OL-086 es distinta de la implementacion OL-079/bit113 ya publicada.
- Una propuesta nueva se prepara aislada; el founder valida UX/UI antes de
  implementar. La rama de implementacion futura se asigna desde el main vigente;
  no arrastrar automaticamente experimentos o decisiones sin aprobar.
- Operador presenta propuesta en su tarea y solicita VoBo; despues entrega SHA,
  archivos, evidencia y limites al gestor. No se vigilan pasos intermedios.
- Durante revision/integracion el SHA entregado queda congelado: no editar esa
  rama a la vez. Hallazgos se devuelven en lote y el gestor libera la rama para
  correccion. Un nuevo pedido del founder se comunica antes de ampliar alcance.
- Gestor resuelve choques y secuencia integracion/publicacion autorizada. PR105
  no se despliega durante la ventana remota de avisos. Pincel permanece local.
- Los operadores no escriben este registro ni la memoria central; cada uno
  modifica solo su bitacora y su entrada de OPEN_LOOPS. Preservar cambios ajenos.

## Fuente Recuperada

Contrastado con el transcript local del gestor Claude
`c45c6c9f-46b4-426d-a884-9ddb0b295fa7` y su memoria
`project-gestion-de-cambios.md`, no solo con nuestras notas posteriores:

- 2026-09-17 20:57:59Z: asigna rama/base, OL/bitacora y archivos que chocan para
  Atras tras entrar; limita alcance y conserva la composicion de otro operador.
- 2026-09-17 20:58:08Z: comunica Cartel en produccion, cierra esa rama y asigna
  la siguiente pieza desde el main publicado, con numeros y migracion reservados.
- 2026-09-17 21:43:01Z: devuelve correcciones verificadas al operador de
  indicadores, pero deja un cambio de portada a decision del founder.
- Memoria original: propuesta documental aislada antes de construir, numeros
  asignados centralmente, rama quieta durante revision, una decision consolidada
  por chat cuando hay mensajes en cola. No reproducir los volumenes antiguos
  de pruebas: se mantiene la regla actual de verificacion proporcional.

## Relevo del gestor (2026-09-21)

El chat gestor `c45c6c9f` (Claude) cierra por tamaño de contexto y entrega a un chat nuevo, «Gestor de cambios II», con las mismas reglas de este registro, de `docs/ops/GESTION_DE_CAMBIOS.md` y de `docs/ops/MEMORIA_GESTOR.md`. El nuevo gestor anota aquí su id de sesión al arrancar.

**Gestor desde el 2026-09-21: sesión Claude `local_ffd34d86-6c74-4f3a-b5f5-8879b4c446ed`, título «Gestor de cambios II»** (modelo efectivo al arrancar: `claude-fable-5-1`, esfuerzo xhigh). Los operadores le escriben con SendMessage por ese título. Al arrancar comprobó que OL-065 ya estaba cerrado: PR #111 unido (`c78d68a`), CI de main en verde, despliegue de producción de `c76b58f` correcto y la rama `topes-de-campos` sin nada fuera de main.

Estado al relevo (main `144d17c` o posterior; producción al día con main; 43 migraciones aplicadas, ninguna pendiente):

- **Publicado esta ronda:** PR #105 y #107 (directorios, OL-085), #108 y #109 (cabecera única y tira de letras, OL-087), #110 (mapa en fichas, OL-089). Todo sin migración. Falta la prueba del founder en su iPhone de los cuatro: tira de letras (aparece al bajar, letra iluminada), cabecera (renglón que se esconde, botón ↑, mapa con «Cercanos»), mapa en fichas de lugar y evento.
- **Topes de campos (OL-065, bitácora 096):** rama `topes-de-campos` en `/Users/apple-1/somosnosotros-topes`, commit `b032ff5` (main unido). El operador Haiku (sesión `local_90c5e65c-4549-476b-b1a9-d5e1c24732b2`) unifica el contador en `ui/ContadorCaracteres` y reporta el hash. **Cerrado el 2026-09-21: PR #111 publicado** (contador unificado, `d9ed621`). Sin migración.
- **Pincel en la app (OL-088, bitácora 123):** EN PAUSA por orden del founder (créditos): rama `pincel-app` en `/Users/apple-1/somosnosotros-pincel-app`, commit `5c42f64`, fase 1 a medias (la bitácora dice dónde quedó). Operador: sesión `local_8b73c722-bf8c-45e1-99e7-518120579a7a`. No retomar hasta que el founder lo pida. El prototipo firmado vive en `codex/pincel-prototipo` (`6063e16`, sin remoto).
- **Avisos (OL-086):** entrega encendida, job `avisos-pendientes` cada 5 minutos desde el 2026-09-18. **El founder confirmó el 2026-09-21 que sí le llegan avisos**; nada pendiente aquí.
- **Limpieza pendiente, con permiso del founder:** ~25 ramas y árboles `codex/*` con commits fuera de main pero ya integrados por contenido (ver `git worktree list`); conservar solo `codex/pincel-prototipo` y `codex/pincel-spike`. También los worktrees de `.claude/worktrees/*` de piezas ya publicadas.
- **Pospuesto por el founder:** panel de admin (explicaciones), rol-de-entonces en destacados, gesto atrás de Safari (OL-071b).
- **Pendientes del founder:** contraseñas filtradas (toggle en Supabase Auth, plan Pro), tope de gasto en Anthropic, restricción del token de Mapbox por URL más alertas de facturación, foto de perfil.
- **Reglas añadidas en esta ronda:** nada de council, workflows ni subagentes sin permiso explícito del founder (costo); operadores en Sonnet 5 o Haiku 4.5, y confirmar el modelo efectivo al arrancar (los chips arrancan en el modelo por defecto); Pincel al final de la cola.

Lo siguiente, en orden: recibir la lista de bugs y mejoras del founder, convertirla en piezas con OL y bitácora, y repartirla a operadores baratos.

**Estado al cierre del 2026-09-22 (madrugada), mismo gestor `local_ffd34d86`:** main y producción al día hasta el PR #150 (`e96bf0e`); **54 migraciones aplicadas**, ninguna pendiente (la del PR #151, `20260922180000_pincel_freno.sql`, ya está en producción). PR #151 (OL-121) publicado (`7f188a1`); ningún PR abierto. Ramas remotas ya unidas a main borradas el 2026-09-22 por orden del founder; las locales y los árboles de trabajo (85) siguen ahí, incluida `codex/pincel-prototipo` (sin remoto, conservar). En curso: OL-116 (renglón de dato + escudo, Haiku 4.5, rama `renglon-dato-alineado`, sin capturas válidas todavía) y OL-111 (analítica de Vercel, sin entrega). Operadores cerrados y sin encargo: Pincel (`local_23fc79ff`), OL-118 (`local_044e2588`), OL-119 (`local_3fa57caa`), OL-121 (`local_d0d193ea`). Cortafuegos de Vercel: dos reglas activas (`/eventos` POST y `/obra`). Detalle de la jornada y pendientes en la bitácora 126. El founder detuvo las pruebas hasta el 2026-09-23.

## Relevo al gestor en la nube (2026-09-24)

**Relevo terminado el 2026-09-25 (founder: «Ya no hay gestor de nube, ahora tú vuelves a estar a cargo de todo»): el gestor local (Gestor de cambios II) lleva otra vez reservas, encargos, revisión y merges. Lo que sigue es histórico.** **Gestor en la nube desde el 2026-09-24: sesión Claude `session_01XrJvzKysk1Y39vzpLGT9HG`** (Claude Code en la web, modelo configurado `claude-fable-5-1`), rama propia `claude/busy-kepler-cf96fb-o3ndxn` para sus anotaciones. Lo puso al tanto el gestor local `local_ffd34d86` por orden del founder: main al día hasta el PR #197 (OL-165), y los guiones `resolver_ol.py` y `resolver_ol_rama_gana.py` subidos al repo en `scripts/ops/` (PR #199) para que el gestor en la nube también los tenga. Siguiente libre: OL-166 / 201.

Lo que este gestor puede hacer desde la nube: revisar entregas que lleguen como rama en GitHub, abrir y unir PR cuando el founder lo pida, comprobar CI y despliegue, mantener OPEN_LOOPS, ASIGNACIONES y la cola, redactar encargos y reservar números. Lo que no puede: mandar mensajes a los chats locales de la Mac, aplicar migraciones con la sesión de Supabase del founder, ni sacar capturas con el Chrome del founder; eso sigue en el gestor local o en el founder. Las reglas son las mismas de este registro, de `GESTION_DE_CAMBIOS.md` y de `MEMORIA_GESTOR.md`.
